[project]
name = "jp-conjugation-backend"
version = "0.1.0"
description = "Backend for the Japanese conjugation practice app"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
