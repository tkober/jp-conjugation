import { WordtypeReadablePipe } from './wordtype-readable.pipe';

describe('WordtypeReadablePipe', () => {
  it('create an instance', () => {
    const pipe = new WordtypeReadablePipe();
    expect(pipe).toBeTruthy();
  });
});
